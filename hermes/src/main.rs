pub mod core;
pub mod utils;

#[tokio::main]
async fn main() {
    todo!("Create the main terminal loop by first requesting a psuedo terminal from master and then communicating via that channel to exchange commands")
}
